<local:BaseDialog
    x:Class="MayazucMediaPlayer.Dialogs.MetadataAsociationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MayazucMediaPlayer.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SET METADATA ASOCIATION"
    PrimaryButtonText="Next"  
    SecondaryButtonText="Cancel">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        
        <StackPanel  HorizontalAlignment="Stretch"  Grid.Row="2">
            <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="WrapWholeWords" Foreground="{StaticResource AppBarItemDisabledForegroundThemeBrush}" Text="Genre association value."/>
            <TextBox  IsEnabled="{Binding IsEnabled}"  Grid.Row="2" Text="{Binding MetadataAsociationValue, Mode=TwoWay}">
            </TextBox>
        </StackPanel>
    </StackPanel>
</local:BaseDialog>
