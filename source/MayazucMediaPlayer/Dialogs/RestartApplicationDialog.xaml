<local:BaseDialog
    x:Class="MayazucMediaPlayer.Dialogs.RestartApplicationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MayazucMediaPlayer.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Restart application?"
    PrimaryButtonText="Restart"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Viewbox Margin="8">
            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Foreground="{StaticResource PinkLogo}" Glyph="&#xE7BA;" />
        </Viewbox>
        <TextBlock Margin="8" Style="{ThemeResource BodyTextBlockStyle}" Grid.Column="1" TextWrapping="WrapWholeWords" Text="The application needs to be restarted to apply your changes. Would you like to restart now?"/>

    </Grid>
</local:BaseDialog>
